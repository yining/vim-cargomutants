tmp/
tags

# artifacts for using covimerage(and coverage.py) for vim test coverage
# and python-poetry is used to create isolated env for covimerage/coverage
# because of the version constraints
poetry.lock
.venv/
# coverage data file
.coverage
.coverage_covimerage
lcov.info
# the html report generated by coverage.py
htmlcov/
